import { ComingInNextSlice } from '@/components/ComingInNextSlice'

export default function SuggestionPage() {
  return (
    <ComingInNextSlice
      title="Make a suggestion"
      summary="An idea for improving how things run. Anonymous by default. A reveal toggle lets you attach your name and one contact detail if you want credit or a reply — it is off unless you turn it on."
    />
  )
}
